{
  "name": "iad.chart",
  "version": "0.1.0",
  "description": "Graphics for IAD",
  "private": true,
  "dependencies": {
    "lodash": "^4.17.11",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8",
    "victory": "^32.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "svg": "mv ./SVG/charts/ ./build",
    "build:all": "CI=true npm run build-svg && npm run build && mv ./SVG/charts ./build/",
    "build-svg": "mkdir -p ./SVG/charts/ && react-scripts test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
